# ReactJS starter code with Webpack and Babel

## Initialize the npm environment
`npm init`

## Install the dependencies
`npm install`

## The command to be run to start the server and other setups
`npm start`

